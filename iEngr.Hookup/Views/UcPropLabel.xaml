<UserControl x:Class="iEngr.Hookup.Views.UcPropLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:iEngr.Hookup.Views"
             xmlns:vm="clr-namespace:iEngr.Hookup.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:PropLabelViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <!-- 创建转换器 -->
        <local:BindingProxy x:Key="Proxy" Data="{Binding}"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dgLabel" Margin="0" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" IsReadOnly="True" 
                  CanUserAddRows="False" AutoGenerateColumns="False" SelectionUnit="FullRow" HorizontalAlignment="Stretch"
                  CanUserSortColumns="False"
                  CanUserReorderColumns="False"
                  ItemsSource="{Binding LabelItems}" 
		          SelectedItem="{Binding SelectedItem}"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  FrozenColumnCount="1"
                 >
            <!-- 行悬停样式 -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="LightSkyBlue"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="5,2,2,2" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="ToolTip" Value="{Binding Path=DisplayName}"/>

                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key" Binding="{Binding Path=Key}"/>
                <!--<DataGridTextColumn Header="标签名称" Binding="{Binding Path=DisplayName}"/>-->
                <DataGridTextColumn Header="节点名称" Binding="{Binding Path=DisplayName}" 
                                    local:Extensions.IsVisible="{Binding Data.IsNoCompared, Source={StaticResource Proxy}}">
                </DataGridTextColumn>
                <DataGridTextColumn Header="节点标签值" Binding="{Binding Path=DisplayValue1}" 
                                    local:Extensions.IsVisible="{Binding Data.IsNoCompared, Source={StaticResource Proxy}}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInherit}" Value="True">
                                    <Setter Property="FontStyle" Value="Italic"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="节点标签值" Binding="{Binding Path=DisplayValue1}" 
                                    local:Extensions.IsVisible="{Binding Data.IsCompared, Source={StaticResource Proxy}}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInherit}" Value="True">
                                    <Setter Property="FontStyle" Value="Italic"/>
                                    <!--<Setter Property="Foreground" Value="Gray"/>-->
                                    <!-- 可以为第二列设置不同的字体 -->
                                    <!--<Setter Property="FontFamily" Value="Arial"/>-->
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsDiff}" Value="True">
                                    <Setter Property="Background" Value="LightCoral"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="项目模板标签值" Binding="{Binding Path=DisplayValue2}"
                                    local:Extensions.IsVisible="{Binding Data.IsCompared, Source={StaticResource Proxy}}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDiff}" Value="True">
                                    <Setter Property="Background" Value="LightCoral"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>