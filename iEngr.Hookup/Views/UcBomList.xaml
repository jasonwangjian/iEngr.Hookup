<UserControl x:Class="iEngr.Hookup.Views.UcBomList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:iEngr.Hookup.Views"
             xmlns:vm="clr-namespace:iEngr.Hookup.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:BomListViewModels />
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dgBOM" Margin="5" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" IsReadOnly="False" 
                  CanUserAddRows="False" AutoGenerateColumns="False" SelectionUnit="FullRow" HorizontalAlignment="Stretch"
                  PreviewKeyDown="dgBOM_PreviewKeyDown"
                  ItemsSource="{Binding DataSource}" 
		          SelectedItem="{Binding SelectedItem}"
                 >
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="CellEditEnding">
                    <i:InvokeCommandAction 
                                Command="{Binding DataContext.CellEditEndingCommand}" 
                                PassEventArgsToCommand="True"/>
                </i:EventTrigger>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction 
                        Command="{Binding DataContext.SelectionChangedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                        PassEventArgsToCommand="True"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <!--<i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction 
                        Command="{Binding DataContext.MouseDoubleClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                        PassEventArgsToCommand="True"/>
                </i:EventTrigger>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction 
                        Command="{Binding DataContext.SelectionChangedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                        PassEventArgsToCommand="True"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>-->
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Path=ID}"  IsReadOnly="True"/>
                <DataGridTextColumn Header="序号" Binding="{Binding Path=No}" IsReadOnly="False"/>
                <DataGridTextColumn Header="Object" Binding="{Binding Path=ObjMat}" />
                <DataGridTextColumn Header="数量" Binding="{Binding Path=Qty}" />
                <DataGridComboBoxColumn Header="供货专业" DisplayMemberPath="Name" SelectedValuePath="Code"
                                        SelectedValueBinding="{Binding Path=SupplyDiscipline}" />
                <DataGridComboBoxColumn Header="供货范围" DisplayMemberPath="Name" SelectedValuePath="Code"
                                        SelectedValueBinding="{Binding Path=SupplyResponsible}" />
                <DataGridComboBoxColumn Header="安装专业" DisplayMemberPath="Name" SelectedValuePath="Code"
                                        SelectedValueBinding="{Binding Path=ErectionDiscipline}" />
                <DataGridComboBoxColumn Header="安装范围" DisplayMemberPath="Name" SelectedValuePath="Code"
                                        SelectedValueBinding="{Binding Path=ErectionResponsible}" />
            </DataGrid.Columns>
        </DataGrid>
        <WrapPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" Margin="0,0,0,5">
            <!--<Button Content="查询" Command="{Binding QueryCommand}"  Margin="5" Width="80"/>
            <Button Content="新增" Command="{Binding NewAddCommand}"  Margin="5" Width="80"/>
            <Button Content="更新" Command="{Binding UpdateCommand}"  Margin="5" Width="80"/>-->
            <Button Content="删除" Command="{Binding DeleteCommand}"  Margin="5" Width="80"/>
        </WrapPanel>
    </Grid>
</UserControl>

