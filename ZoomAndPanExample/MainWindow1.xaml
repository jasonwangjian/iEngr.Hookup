<Window x:Class="ZoomAndPanExample1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="图像查看器" Height="600" Width="800">
    <!--Loaded="Window_Loaded">-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <!--<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="打开图像" Click="OpenImage_Click" Margin="5" Padding="10,5"/>
            <Button Content="放大" Click="ZoomIn_Click" Margin="5" Padding="10,5"/>
            <Button Content="缩小" Click="ZoomOut_Click" Margin="5" Padding="10,5"/>
            <Button Content="重置" Click="Reset_Click" Margin="5" Padding="10,5"/>
            <Button Content="适应窗口" Click="FitToWindow_Click" Margin="5" Padding="10,5"/>
            <TextBlock Text="缩放:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <Slider x:Name="ZoomSlider" Minimum="10" Maximum="400" Value="100" 
                    Width="100" ValueChanged="ZoomSlider_ValueChanged"/>
            <TextBlock x:Name="ZoomText" Text="100%" Width="40" VerticalAlignment="Center" Margin="5,0"/>
        </StackPanel>

        --><!-- 使用 Border 作为可交互的容器 --><!--
        <Border Grid.Row="1" Background="LightGray" 
        PreviewMouseWheel="Border_PreviewMouseWheel"
        PreviewMouseLeftButtonDown="Border_PreviewMouseLeftButtonDown"
        PreviewMouseLeftButtonUp="Border_PreviewMouseLeftButtonUp"
        PreviewMouseMove="Border_PreviewMouseMove"
        Cursor="Arrow">

            <ScrollViewer x:Name="viewer" 
                          HorizontalScrollBarVisibility="Auto" 
                          VerticalScrollBarVisibility="Auto"
                          Background="DarkGray">

                <Viewbox x:Name="viewbox" Stretch="Uniform">
                    <Image x:Name="contentImage" Source="E:\\myTemp\\TestImg.jpg" 
                           Stretch="None" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Viewbox>

            </ScrollViewer>
        </Border>-->
    </Grid>
</Window>